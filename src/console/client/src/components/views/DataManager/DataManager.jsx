import React from 'react';
import { exit } from '../../../utils';


export const DataManager = () => {
  return (
    <div>
      Data Manager
      <button>
        Exit process
      </button>
    </div>
  )
}